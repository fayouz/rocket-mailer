# In all environments, the following files are loaded if they exist,
# the latter taking precedence over the former:
#
#  * .env                contains default values for the environment variables needed by the app
#  * .env.local          uncommitted file with local overrides
#  * .env.$APP_ENV       committed environment-specific defaults
#  * .env.$APP_ENV.local uncommitted environment-specific overrides
#
# Real environment variables win over .env files.
#
# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE NOR IN ANY OTHER COMMITTED FILES.
# https://symfony.com/doc/current/configuration/secrets.html
#
# Run "composer dump-env prod" to compile .env files for production use (requires symfony/flex >=1.2).
# https://symfony.com/doc/current/best_practices.html#use-environment-variables-for-infrastructure-configuration

###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=
APP_SHARE_DIR=var/share
###< symfony/framework-bundle ###

###> symfony/routing ###
# Configure how to generate URLs in non-HTTP contexts, such as CLI commands.
# See https://symfony.com/doc/current/routing.html#generating-urls-in-commands
DEFAULT_URI=http://localhost
###< symfony/routing ###

###> doctrine/doctrine-bundle ###
# Format described at https://www.doctrine-project.org/projects/doctrine-dbal/en/latest/reference/configuration.html#connecting-using-a-url
# IMPORTANT: You MUST configure your server version, either here or in config/packages/doctrine.yaml
#
# DATABASE_URL="sqlite:///%kernel.project_dir%/var/data_%kernel.environment%.db"
# DATABASE_URL="mysql://app:!ChangeMe!@127.0.0.1:3306/app?serverVersion=8.0.32&charset=utf8mb4"
# DATABASE_URL="mysql://app:!ChangeMe!@127.0.0.1:3306/app?serverVersion=10.11.2-MariaDB&charset=utf8mb4"
DATABASE_URL="postgresql://app:app@127.0.0.1:5432/app?serverVersion=16&charset=utf8"
###< doctrine/doctrine-bundle ###

###> nelmio/cors-bundle ###
CORS_ALLOW_ORIGIN='^https?://(localhost|127\.0\.0\.1)(:[0-9]+)?$'
###< nelmio/cors-bundle ###

###> lexik/jwt-authentication-bundle ###
JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
JWT_PASSPHRASE=440ca1dcce83771141fabb0091f89742e5ad1ee2f429236b9ec12586c9cffbeb
###< lexik/jwt-authentication-bundle ###

###> symfony/mailer ###
MAILER_DSN=null://null
###< symfony/mailer ###

###> symfony/messenger ###
# Choose one of the transports below
# MESSENGER_TRANSPORT_DSN=amqp://guest:guest@localhost:5672/%2f/messages
# MESSENGER_TRANSPORT_DSN=redis://localhost:6379/messages
MESSENGER_TRANSPORT_DSN=doctrine://default
###< symfony/messenger ###

###> symfony/lock ###
# Choose one of the stores below
# postgresql+advisory://db_user:db_password@localhost/db_name
LOCK_DSN=flock
###< symfony/lock ###

###> app ###
# URL of the Nuxt front (used for CORS and embed links)
FRONTEND_URL=http://localhost:3000
# Default "From" at install ("Name <email>" or "email"). It seeds the sender addresses of the
# settings on first use; administrators then manage them in the app (Réglages).
# First-run setup: until a user exists, /setup creates the administrator. If the instance is reachable
# by others before you set it up, set a secret here: the setup page will ask for it.
SETUP_TOKEN=

# Sending mailboxes: key encrypting their passwords in the database (empty: derived from APP_SECRET).
# Changing it makes the stored passwords unreadable: they must then be entered again.
MAILBOX_ENCRYPTION_KEY=
MAILBOX_TIMEOUT=15

MAILER_DEFAULT_FROM="Rocket Mailer <no-reply@example.com>"
# Lifetime (seconds) of embed tokens minted by applications.
EMBED_TOKEN_TTL=900
# LDAP directory: default configuration. Administrators can then change it in Administration → Annuaire LDAP
# (stored in the database, password encrypted); "Revenir aux valeurs par défaut" goes back to these values.
LDAP_ENABLED=0
LDAP_URL=ldap://localhost:389
# STARTTLS on an ldap:// URL (use ldaps:// for implicit TLS).
LDAP_START_TLS=0
LDAP_BASE_DN="ou=people,dc=example,dc=org"
LDAP_SEARCH_DN="cn=admin,dc=example,dc=org"
LDAP_SEARCH_PASSWORD=
LDAP_USER_FILTER="(objectClass=inetOrgPerson)"
# Members of this group get ROLE_ADMIN; leave empty to manage admins in the app.
LDAP_ADMIN_GROUP_DN=
# Attributes read from the entries (e.g. Active Directory: userPrincipalName or mail, givenName, sn, memberOf).
LDAP_ATTRIBUTE_EMAIL=mail
LDAP_ATTRIBUTE_FIRST_NAME=givenName
LDAP_ATTRIBUTE_LAST_NAME=sn
LDAP_ATTRIBUTE_GROUPS=memberOf
# Version of the build ("git describe --tags", set by the Docker image). Empty: "dev".
APP_VERSION=
# Updates: GitHub repository whose releases are checked (empty: no check).
UPDATE_REPOSITORY=fayouz/rocket-mailer
# One-click update: HTTP API of the "updater" service (Watchtower) of compose.yaml, and its token.
UPDATER_URL=
UPDATER_TOKEN=
# Update method: docker (UPDATER_*), script (servers without Docker) or manual. Empty: docker if UPDATER_TOKEN is set,
# else manual. Administrators can change it in Administration → Mises à jour.
UPDATE_METHOD=
# Script method: run by the scheduled task `bin/console app:update:run` (every minute, as the owner of the files).
UPDATE_SCRIPT=%kernel.project_dir%/../deploy/update.sh
# Command restarting the services once updated, run by the script, e.g. "sudo systemctl restart rocket-mailer-front".
UPDATE_RESTART_COMMAND=
UPDATE_SCRIPT_TIMEOUT=1800
# Attachments: directory shared by the API and the worker, max size per file and per email (bytes).
ATTACHMENTS_DIR=%kernel.project_dir%/var/attachments
ATTACHMENT_MAX_SIZE=10485760
ATTACHMENTS_MAX_TOTAL_SIZE=26214400
###< app ###

###> symfony/amazon-mailer ###
# MAILER_DSN=ses://ACCESS_KEY:SECRET_KEY@default?region=eu-west-1
# MAILER_DSN=ses+smtp://ACCESS_KEY:SECRET_KEY@default?region=eu-west-1
###< symfony/amazon-mailer ###

###> symfony/brevo-mailer ###
# MAILER_DSN=brevo+api://KEY@default
# MAILER_DSN=brevo+smtp://USERNAME:PASSWORD@default
###< symfony/brevo-mailer ###

###> symfony/mailgun-mailer ###
# MAILER_DSN=mailgun+smtp://USERNAME:PASSWORD@default?region=us
# MAILER_DSN=mailgun+https://KEY:DOMAIN@default?region=us
# MAILER_DSN=mailgun+api://KEY:DOMAIN@default?region=us
###< symfony/mailgun-mailer ###

###> symfony/mailjet-mailer ###
# MAILER_DSN=mailjet+api://PUBLIC_KEY:PRIVATE_KEY@api.mailjet.com
# #MAILER_DSN=mailjet+smtp://PUBLIC_KEY:PRIVATE_KEY@in-v3.mailjet.com
###< symfony/mailjet-mailer ###

###> symfony/postmark-mailer ###
# MAILER_DSN=postmark://ID@default
###< symfony/postmark-mailer ###

###> symfony/sendgrid-mailer ###
# MAILER_DSN=sendgrid://KEY@default
###< symfony/sendgrid-mailer ###
